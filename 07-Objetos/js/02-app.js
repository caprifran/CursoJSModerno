const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true
}


console.log(producto);

// Sintaxis de punto
console.log(producto.nombre);

// Corchetes
console.log(producto['nombre']);