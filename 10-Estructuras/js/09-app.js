// Operador terniario

const autenticado = true;
const puedePagar = true;

console.log( autenticado ? puedePagar ? "Si esta autenticado y puede pagar" : "No, no esta autenticado" : "No no esta autenticado" );
